---
paths:
  - "**/*.py"
---

# Python 코드 규칙

<!-- 예시 규칙입니다. 팀 컨벤션에 맞게 수정하세요.
     PEP 8과 동일한 항목은 삭제해도 됩니다 — 모델이 기본으로 따릅니다. -->

## 네이밍

- 모듈/함수/변수: snake_case, 클래스: PascalCase, 상수: UPPER_SNAKE_CASE
- 도메인 용어는 영어로 번역해 사용하라(로마자 표기 금지):
  주문 → order (jumun ×), 정산 → settlement
- 불리언은 is_/has_/can_ 접두사를 사용하라.

## 스타일

- 타입 힌트를 모든 공개 함수 시그니처에 붙여라.
- 포매터/린터 설정(pyproject.toml)이 있으면 그것이 우선한다.
