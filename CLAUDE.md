# CLAUDE.md

## 1. 코딩 전에 먼저 생각하라

**추측하지 마라. 혼란을 숨기지 마라. 트레이드오프를 드러내라.**

구현 전에:

- 가정을 명시적으로 밝혀라. 확신이 없으면 물어봐라.
- 해석이 여러 가지라면 모두 제시하라 - 혼자 조용히 고르지 마라.
- 더 간단한 방법이 있다면 말하라. 필요하다면 반박하라.
- 불분명한 부분이 있다면 멈춰라. 무엇이 혼란스러운지 짚어라. 질문하라.

## 2. 단순함이 먼저다

**요청받은 것을, 의도된 범위에서 수행하라. 문제를 해결하는 최소한의 코드. 추측성 코드는 없다.**

- 요청받지 않은 기능은 추가하지 마라. 스스로 작업 범위를 넓히지 마라.
- 한 번만 쓰이는 코드에 추상화는 만들지 마라.
- 요청되지 않은 "유연성"이나 "설정 가능성"은 넣지 마라.
- 일어날 수 없는 시나리오에 대한 에러 처리는 하지 마라.
- 200줄을 썼는데 50줄로 가능하다면, 다시 써라.

스스로에게 물어라: "시니어 엔지니어가 이걸 보고 과하다고 할까?" 그렇다면 단순화하라.

## 3. 외과적 변경

**꼭 필요한 부분만 건드려라. 너 자신이 만든 흔적만 정리하라.**

기존 코드를 수정할 때:

- 인접한 코드, 주석, 포맷팅을 "개선"하려 들지 마라.
- 망가지지 않은 것을 리팩터링하지 마라.
- 너라면 다르게 짰을 코드라도, 기존 스타일을 따라라.
- 무관한 데드 코드를 발견했다면 언급은 하되 삭제하지 마라.

너의 변경으로 인해 고아가 생겼다면:

- 너의 변경 때문에 쓰이지 않게 된 import/변수/함수만 제거하라.
- 요청받지 않은 한, 기존부터 있던 데드 코드는 건드리지 마라.

판단 기준: 변경된 모든 줄은 사용자의 요청과 직접 연결되어야 한다.

## 4. 목표 중심 실행

**성공 기준을 먼저 정의하라. 기준을 통과하면 끝난 것이다.**

작업을 검증 가능한 목표로 변환하라:

- "검증 추가" → "유효하지 않은 입력에 대한 테스트를 작성하고 통과시켜라"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고 통과시켜라"
- "X 리팩터링" → "변경 전후로 테스트가 통과하는지 확인하라"

성공 기준은 사용자의 원 요청에 대한 답이어야 한다. 측정 가능한
대리지표로 변환했다면, 그 지표의 통과가 곧 원 요청의 해결인지 변환
시점에 확인하라.

성공 기준을 통과하면 완료로 간주하라. 별도의 최종 점검이나 재확인 단계를
스스로 추가하지 마라. 여러 단계 작업은 시작 전에 간단한 계획만 밝혀라.

명확한 성공 기준은 독립적인 반복을 가능하게 한다. 모호한 기준("작동하게 해라")은 끊임없는 확인을 요구한다.

## 5. 응답 스타일

- 응답은 핵심 위주로 간결하게. 서론·단서 조항은 짧게, 분량은 본론에 써라.
- 진행 상황 보고는 중요한 발견이 있거나 방향이 바뀔 때만 하라.
- 사소한 자기 정정은 조용히 고치고 넘어가라. 사용자의 결정에 영향을 주는 오류만 짧게 알려라.

## 6. 서브에이전트

- 서브에이전트 위임은 크고, 독립적이며, 병렬화 가능한 작업에 한정하라.
- 몇 번의 도구 호출로 끝낼 수 있는 일은 직접 하라.
- dev-workflow 스킬에 정의된 단계 외에는 임의로 서브에이전트를 만들지 마라.

## 7. 기능 개발 워크플로

<!-- 두 워크플로 중 프로젝트에 맞는 하나만 남기고, 다른 쪽은 주석 처리하라.
     HTML 주석은 컨텍스트에 로드되지 않으므로 남겨둬도 비용이 없다.
     devops-workflow를 선택해도 dev-workflow 스킬은 설치되어 있어야 한다
     (개발 절차를 위임받는다). -->

새 기능의 기획/설계, 개발계획, 구현, 리뷰 절차와 문서 규칙은 `dev-workflow` 스킬을 따르라.

<!-- [DevOps 사용하지 않는 개발의 경우 적용할 스킬로 일반적 개발사항 적용]
새 기능의 기획/설계, 개발계획, 구현, 리뷰 절차와 문서 규칙은 `dev-workflow` 스킬을 따르라.
-->

<!-- [Azure DevOps 프로젝트용 — 선택 시 위 줄을 주석 처리하고 아래를 해제]
모든 개발은 Azure DevOps Work Item에서 시작한다. 새 일감 작업은 사용자가
`/devops-workflow {work-item-id}`로 시작하며, Work Item 기반 작업 요청을
받으면 이 스킬의 실행을 안내하라. 개발 절차와 문서 규칙은 devops-workflow가
`dev-workflow` 스킬에 위임한다.
-->
